# Installed dependencies and generated output
node_modules/
build/
dist/
.next/
.wrangler/
coverage/
# Runtime records contain encrypted OAuth credentials and business audit data.
# They belong on private persistent volumes, never in a source commit.
/data/
/runtime/dispatch-center/data/

# Local configuration and diagnostics
.env
.env.*
!.env.example
*.log
.DS_Store
