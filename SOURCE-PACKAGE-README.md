# 直播中心工作台历史完整源码与空数据模板

本项目来自本地 `直播中心工作台功能升级-20260829/candidate-source`，其中已有 9 月 7 日增量源码和测试。包括主服务、lifecycle-engine、site、协作中心、数据中心、调度中心、数据库 schema/迁移、构建脚本与历史模块 HTML/JS。

必须说明版本边界：2026-09-14 只读核查时，原 `fd-027340-live-center-workbench` 容器、对应 runtime 目录和端口 24500 监听均未找到； nginx 仍留有该路由。因此不能声明本目录与当前线上运行版本匹配，也不能将公司登录跳转视为业务可用。此前发布记录属于历史证据。

## 数据与模板

本分发副本保留渲染、流程和人员策略源码，但未包含生产数据库、业务导出、用户照片、运行环境和凭据。原始采集目录未修改。

- `anchor-profiles.json`、`anchor-data.js`、`anchor-trends.js` 是新增空模板；空值表示待接入真实源，不能当成真实人数为零。
- 早报页面中的历史照片、GMV、排班、业绩报告和报告追加语句已改为明确的空数据模板；招聘页面的候选人、面试事件和快照日期也已清空。
- 主播照片与 sprite 未收录。相关页面需要重新绑定本环境授权的图片资源。
- 两个 runtime 子项目的全部生成 dist 均未收录，其中生成的 `prerenderSecret` 不应随源码分发。重新构建时由构建工具生成新的值，不复用历史值。
- `DISTRIBUTION-TEMPLATE-CHANGES.json` 记录分发副本的静态模板改动。源代码中明确的默认配置/策略保留，不把它们当作当前经营事实。

## 构建与运行

主项目使用 Node.js 22，源码根没有 package.json；主服务使用 Node 内置模块。协作中心、数据中心各自有 package.json 和锁文件，可分别在其目录运行 `npm ci`、`npm run build`。`runtime/dispatch-center` 的 Node 服务/测试另行运行。

根 Dockerfile 会构建协作中心后打包主服务：`docker build -t wis-live-center:source .`。其他旧 `Dockerfile.*` 或 deploy 脚本包含历史私有镜像/路径，是原工程源码，不代表可直接运行的当前发布器。不要原样执行历史生产发布脚本。

公司中央鉴权、飞书应用、根数据、回流 API、持久化路径、原数据/调度服务需另行配置。只有空模板的页面不构成业务验收；本次未重新安装所有依赖、部署或运行业务。
