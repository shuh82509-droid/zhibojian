# 直播中心早报看板导出

导出版本：`3f2b34c`（本任务中最后一次已发布的源码状态）。

## 入口

- `index.html`：单页早报看板入口，包含 2026.08.07—2026.08.13 的页面数据和前端样式/逻辑。
- `package.json`、`package-lock.json`：原 HTML 应用的最小项目元数据；无本地 npm 依赖。

## 运行方式

该版本在妙搭 HTML 应用运行时中可直接运行。图表脚本来自 ECharts CDN；主播头像、GMV 快照、轮转图和视觉原图均继续使用页面内的妙搭文件存储路径。完整资源清单见 `assets/RESOURCE_MANIFEST.md`。

本次仅恢复导出文件：未读取或更新飞书数据，未部署，也未修改线上应用。
